const player = document.getElementById("player")

function playSong(id) {
return  player.src = `https://www.youtube.com/embed/${id}`
  // Challenge: Add code here to make the youtube player play the new YouTube song
}